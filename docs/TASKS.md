# TASKS

## 当前阶段

Music 播放列表开关、线上发现、歌词、快捷启动、临时暂存和设置偏好阶段已完成基础接入。当前重点是保持顶部 island 体验稳定，继续小步处理交互、视觉和文档同步。

## 正在做

- [ ] 继续观察 compact / expanded 顶部贴边和单层 island 壳体是否稳定，并持续优化微观交互
- [ ] 临时暂存后续再评估跨启动恢复及 bookmark 持久化策略
- [ ] Music 后续阶段再评估专辑封面、系统媒体键或更完整资料库
- [ ] 代码审查跟进：
  - [x] 收紧 `LyricsService` 自动联网歌词缓存条件：仅高可信标题 / 歌手匹配自动下载缓存，低可信结果回退候选选择
  - [x] 清理 `CompactIslandView` 中不再暴露的旧 Mascot 偏好读取，或明确保留为兼容路径
  - [x] 统一 TemporaryTray 代码注释和状态文案为“临时暂存”
  - [x] 将 `StatusBarOnionMark.imageset` 接入菜单栏图标，并清理未使用的 `StatusBarOnion.imageset`
  - [x] 修复在线歌曲在“单曲循环”下自然结束后不重播当前曲目的问题
  - [x] 修复播放列表恢复时失效 URL 被过滤后 `currentIndex` 指向错误歌曲的问题
  - [x] 抽出 `OnlineMusicService`，收拢网易云用户歌单、播放 URL 和边听边存触发职责
  - [x] 同步歌词触发时机、线上发现、自动监听和当前角色数量等文档漂移
  - [x] 明确线上发现歌曲列表和播放失败状态只做当前 App 会话缓存，退出或刷新后清空，不进入播放列表持久化

## 已完成

- [x] 新增默认输出设备恢复：
  - [x] 在局域网遥控网页提供“输出设备”菜单，枚举当前系统可见的输出设备并保存设备 UID
  - [x] 在局域网遥控网页提供“输出设备”菜单和“恢复”按钮
  - [x] 选择设备后立即尝试切换系统默认输出
  - [x] App 启动和 Mac 唤醒后延迟重试恢复已保存的输出设备，用于蓝牙音箱重新可见后的自动切回
- [x] 新增无配对局域网网页遥控器：
  - [x] 新建 `RemoteControlServer`，通过 `Network` 框架监听 `17777` 端口
  - [x] 提供手机 / 浏览器网页控制播放 / 暂停、上一首 / 下一首、音量和播放列表点击播放
  - [x] 遥控网页通过“歌词 / 歌曲列表”分栏同步显示当前歌词、前后几行和播放列表；在线候选确认仍在 Mac 端完成
  - [x] 提供“让 Mac mini 睡眠”按钮，触发当前运行 Onion 的 Mac 进入系统睡眠
  - [x] 菜单栏显示当前遥控器访问地址
  - [x] 为 Sandbox 增加 incoming network server 权限
- [x] 打包并发布 Release 版本：
  - [x] 在 Release 配置下成功完成项目的构建和归档（`xcodebuild archive`）
  - [x] 成功提取归档中的 `.app` 应用程序包并压缩打包为 `OnionFlow-Release.zip`
  - [x] 发布压缩包属于本地交付产物，不保留在仓库根目录
- [x] 将状态栏菜单翻译为中文，并使用 SF Symbols 和快捷键美化 UI：
  - [x] 重构菜单为支持动态响应的 `MenuBarContent` 子视图
  - [x] 当有正在播放/加载的音乐时，动态显示当前曲目和播放控制选项（播放/暂停、上一首、下一首）
  - [x] 将菜单栏文字翻译为中文（“收起 Onion 面板”/“展开 Onion 面板”、“设置与偏好...”、“退出 Onion”），并分别使用 `chevron.up/down.circle`、`gearshape` 和 `power` 进行视觉美化
  - [x] 为菜单项添加 Command 组合快捷键（`Cmd+E`、`Cmd+,`、`Cmd+Q`），提升操作便利性
- [x] 为 App 与状态栏重新设计 Claude Code 风格极简像素实心圆（Onion）图标：
  - [x] 重构状态栏 SVG 图标 `status-bar-onion-mark.svg` 为极简的 18x18 8-bit 点阵像素实心圆
  - [x] 使用 Swift 脚本动态生成全套尺寸的 App Icon PNG，采用 macOS 圆角矩形深色终端渐变底盘，并在中心以点阵像素格与格栅间隙绘制发光的翡翠绿像素实心圆，更新资产目录 `Contents.json`
- [x] 新增 3 个 Claude Code 风格满屏保护区矢量吉祥物角色（像素猫、像素恐龙、终端蛙）：
  - [x] 新建并实现了 3 个以 Canvas 高保真矢量组件绘制的角色动效 View：像素猫 (`PixelCatMascotView.swift`)、像素恐龙 (`PixelDinoMascotView.swift`) 与终端蛙 (`PixelFrogMascotView.swift`)
  - [x] 在 `MascotKind`、`MascotView` 和设置页 `glowColor` 方法中分别挂载了这 3 个新角色的枚举、路由分发与独特的霓虹散射发光色
- [x] 从角色系统中完全移除“小章鱼”角色：
  - [x] 从 `MascotKind`、`MascotView` 和设置页角色卡片中移除 `.octo` 枚举、分发与配色
  - [x] 物理删除对应的 `Characters/PixelOctoMascotView.swift` 源码文件
- [x] 从角色系统中完全移除原版“小狗”角色（保留“像素小狗”）：
  - [x] 从 `MascotKind`、`MascotView` 和设置页角色卡片中移除 `.sleepCapsule` 枚举、分发与配色
  - [x] 将默认/兜底角色偏好统一修改为 `.pixelPuppy`（像素小狗）
  - [x] 物理删除对应的 `Characters/SleepCapsuleMascotView.swift` 源码文件
- [x] 从角色系统中完全移除“阿飘”角色：
  - [x] 从 `MascotKind`、`MascotView` 和设置页角色卡片中移除 `.pixelGhost` 枚举、分发与配色
  - [x] 物理删除对应的 `Characters/PixelGhostMascotView.swift` 源码文件
- [x] 删除角色系统中的旧版小羊、小鸟、木头人、小洋葱和旧版小猫：
  - [x] 从 `MascotKind`、`MascotView` 和设置页角色卡片中移除对应枚举、分发和配色
  - [x] 删除对应旧版 `Characters/` 角色 View 文件，并保留小机器人、鬼魂、像素螃蟹、像素小狗、像素猫、像素恐龙和终端蛙
- [x] 设置页高度改为内容自适应：
  - [x] `MascotPickerView` 移除固定高度，通过内容测量回传实际高度
  - [x] `SettingsWindowController` 根据内容高度重设 `NSPanel`，并保持设置窗口顶部锚点不跳动
- [x] 将 Shiba 小狗（柴犬）重构为复古 8-bit 像素小狗（Pixel Puppy）：
  - [x] 使用 `PixelPuppyMascotView.swift` 承载当前像素小狗角色，基于 `16x12` LED 发光点阵 Canvas 格栅，设计乖巧坐姿（Frame A）、招风耳兴奋竖起（Frame B）、奔跑张扬腿（Frame C）三套液晶点阵图，渲染时同样扣除 `0.22pt` 格栅微缝，配置高雅的太妃金黄色（`Color(red: 0.98, green: 0.74, blue: 0.18)`）。
  - [x] 重写动态表情：`idle` 状态下小爪与耳朵微晃呼吸假寐，飘像素 Z 字符；`working` 状态下双耳快速上下耸动，头顶点亮 8-bit 进度点；`music` 状态下双耳激情摆动，并结合旋转方程式做可爱的 **斜角点头与抖耳** 魔性舞蹈，完美呼应像素螃蟹，统一复古电子宠物美学。
- [x] 新增致敬 Claude Code 经典宠物 Clawd 风格的复古 8-bit 像素螃蟹（Pixel Crab）：
  - [x] 新建 `PixelCrabMascotView.swift`，基于 `16x12` 像素格点阵在 Canvas 中设计平滑防守（Frame A）、双螯挥舞上张（Frame B）、奔跑跃动（Frame C）三套发光点阵，渲染时扣减 `0.22pt` 格栅黑空隙，完美复刻 GameBoy/液晶电子宠物屏独特的“物理格栅黑线”荧光质感，配置亮丽发光红橘色。
  - [x] 编写动态交互：`idle` 闲时眨眼、飘像素 Z 字符，并吐出一连串大小不一的 **8-bit 中空像素小气泡**；`working` 时小短腿高频跑步倒腾、悬浮像素 loading 点；`music` 状态下双螯交替举手，并编写 **“经典螃蟹步横行踱步”** X 轴物理位移方程（`sin(t * 4.0) * 3.5`），使其在灵动岛内左右魔性踱步爬行。
  - [x] 在 `MascotKind.swift` 中新增 `.pixelCrab` 成员并映射显示名称为 `"像素螃蟹"`；在 `MascotView.swift` 统一分发中路由挂载；在 `MascotPickerView.swift` 中为其分配霓虹红橘散射发光背光（`Color(red: 0.98, green: 0.36, blue: 0.26)`）。
- [x] 确立紧凑、精致的角色常驻排版（撤回满屏大尺寸，回归原先微观美学）：
  - [x] 在 `MascotPickerView.swift` 和 `CompactIslandView.swift` 中，将测试性质的满屏大尺寸角色还原。恢复为原先 `32x32` 黑色微盒包裹 `28pt` 超精细卡片头像，以及紧凑岛内 `32` 尺寸配合 `clipped()` 裁切约束的经典设计，保持 Onion 独特的精细、克制与呼吸质感。
- [x] 当前 7 款吉祥物角色完成统一接入：
  - [x] 当前 `MascotKind` 保留小机器人、鬼魂、像素螃蟹、像素小狗、像素猫、像素恐龙和终端蛙
  - [x] 每个角色由 `MascotView` 分发到 `Characters/` 下对应 View，并在设置页自动展示
  - [x] 已移除的小羊、小鸟、木头人、小洋葱和旧版小猫 / 小狗文件不再作为当前源码职责记录
- [x] 设置窗口不透明度视觉优化：
  - [x] 在 `MascotPickerView.swift` 中，将关闭“氛围背景”时的半透明磨砂毛玻璃（ultraThinMaterial + opacity(0.65)）移除，改为极富质感、完全不透明的深炭灰纯色背景（`Color(red: 0.04, green: 0.04, blue: 0.06)`），满足不透明的要求，同时在 AppKit 窗体底层维持透明属性以保证窗口 16pt 大圆角边缘完美平滑且无系统黑色直角裁切。
- [x] 新增 4 种灵动岛收起态右侧的频谱物理律动动画，并集成于设置面板进行无缝切换与跨启动持久化：
  - [x] 新建 `SpectrumStyle.swift` 规范频谱风格枚举类型并支持 `@AppStorage` 存储
  - [x] 在 `CompactMusicActivityIndicator.swift` 扩展 Canvas 并实现四种高精动效（翡翠晶柱、极极简声波、灵动呼吸、科幻脉冲）的物理律动方程，完美适配播放中振翼与暂停回弹极简状态
  - [x] 在 `MascotPickerView.swift` 中嵌入“频谱风格”段选择器，设计为优雅的单色灰度（Monochromatic Gray）微小部件，并微调面板内容布局
  - [x] 在 `SettingsWindowController.swift` 保持无边框设置窗口固定宽度，并通过内容自适应高度容纳角色、频谱、偏好和性能选项
- [x] 产品正式名称调整为 `Onion Flow`，紧凑界面使用简称 `Onion`；工程、target / scheme、Bundle ID 与数据目录统一为 `OnionFlow`
- [x] 设置窗口高度排版优化、角色预览与悬停微交互动效：
  - [x] 优化卡片间距与排版，将设置窗口改为 `440pt` 固定宽度和内容自适应高度，为分组后的设置项保留稳定布局空间
  - [x] 重构设置窗口网格卡片为自适应弹性布局，使角色与频谱卡片左右侧边缘与下方偏好设置卡片 100% 绝对对齐，统一左右物理边距（24pt）
  - [x] 给 7 个 Mascot 角色 View 保留 `isStatic` 支持，并按当前交互需求在设置卡片中恢复 `idle` 动画预览
  - [x] 实现氛围背景 10 种配色选择按钮的 Hover 视差与微动效（悬停时 `1.15` 倍放大、白边发光与阴影辉光，选中时 `1.08` 倍放大），提升卡片交互精致度
  - [x] 对设置项说明文本应用多行自动折行逻辑，并在“动态粒子”旁展示清晰的 GPU 性能占用提示
- [x] 设置窗口重构与背景动画开关控制：
  - [x] 在 `ExpandedIslandView.swift` 中通过 `@AppStorage` 条件分支按需挂载/卸载 `AntigravityBackgroundView`，关闭星云与粒子时回到静态暗色背景
  - [x] 在 `MascotPickerView.swift` 中重构网格布局为紧凑的 4 列网格（2行，从 114×68 优化为 82×66），释放下部空间
  - [x] 在 `MascotPickerView.swift` 底部新增「偏好设置」卡片容器，提供“氛围背景”和默认关闭的“联网匹配歌词”开关
  - [x] 删除无效的背景拖动和动态高度桥接，仅在设置页标题栏提供窗口拖动区域
  - [x] 设置页改为完整接收鼠标事件的可交互面板，并修复标题栏拖动区域失效
  - [x] 关闭设置 `NSPanel` 原生阴影，修复窗口覆盖顶部 island 时已复现的 GPU 升高问题
  - [x] 汉化设置页的英文标题，并利用根容器的全局 `.onHover` 指针重置机制彻底解决文本悬停导致 cursor 变为 i 状态的体验 Bug
  - [x] 设置页标题调整为“Onion 设置”，副标题覆盖角色、外观与播放器功能配置范围
  - [x] 设置窗口按灵动岛壳体定位，顶部下移 `3pt`，右侧视觉贴合位置与 expanded 投影预留保持一致
  - [x] Onion 设置按钮支持再次点击关闭已打开的设置窗口
  - [x] 设置按钮提示词随窗口可见状态切换为“打开设置”或“关闭设置”
  - [x] 灵动岛收起时同步关闭仍在显示的设置窗口
  - [x] 新增「性能与效果」卡片分组，将动态粒子开关与 GPU 占用提示从通用偏好中独立展示
  - [x] 增加「播放列表」开关，展开并关闭播放列表时只保留歌曲信息、播放控制、进度条和当前歌词单行，并保持单层壳体结构
  - [x] 修复切换「播放列表」开关后设置窗口与 island 的视觉间隔不一致问题
  - [x] 优化关闭播放列表后底部单行歌词的视觉体验：字号从 11pt 缩减为 10pt，字重改为常规字重（非加粗），并将其重构移动到底部 overlay 居中悬浮展示，保留 20pt 底部安全间距，规避圆角裁剪且不影响其它 UI 布局与窗体阴影。
  - [x] 优化双功能关闭状态下的留白视觉比例：当同时关闭「快捷启动」与「临时暂存」时，调整其高度计算规则使灵动岛展开态高度收敛为 `365pt`，将播放列表卡片底部的物理总留白提升并固定为更优雅的 `63.4pt`，彻底消除过矮和紧绷感，达到完美的视觉平衡比例，且通过高度自适应联动，完全不影响圆角裁剪与阴影渲染。
  - [x] 迭代升级云端歌词自动匹配门禁（isTrustedMatch）系统：重构高可信判定算法，引入「多歌手合唱交叉验证」及「歌名正则去噪比对」（自动剥离轨道号前缀、各种中英文括号后缀等干扰项），完美解决了如《是妈妈是女儿》等联唱歌曲及带有多余噪音前/后缀的本地音乐无法静默秒配歌词的体验硬伤，实现了 100% 的高可信自动秒配下载。
- [x] 顶层 island 渲染拆分：
  - [x] 将 compact 顶栏拆入 `CompactIslandView.swift`
  - [x] 将 expanded 背景、空白收起手势和播放器承载拆入 `ExpandedIslandView.swift`
  - [x] 将 notch-style 外轮廓拆入 `NotchIslandShape.swift`
  - [x] `ContentView.swift` 保持单个 island 壳体组合、两阶段展开动画和预览职责
- [x] 修复 `VinylRecordView` 旋转动画失效的 Bug，并进行 GPU 渲染分层优化：
  - [x] 采用时间戳计算旋转角度差值的无副作用设计，解决了由于在 SwiftUI 渲染求值期间修改 `@State` 导致被系统冻结而不旋转的生命周期冲突问题，并实现了暂停时精准停靠、播放时平滑接续
  - [x] 将黑胶唱片拆分为静态背景层（只在视图出现时渲染一次，0帧重绘负担）与高频重绘点（仅重绘并旋转 3x3 偏心点），节省了 95% 以上的唱片旋转 GPU 开销
- [x] 歌词标签页按需加载与展示歌词：
  - [x] 开启 Sandbox 网络客户端访问权限（修改 `OnionFlow.entitlements`）
  - [x] 实现 `LrcParser` 模型解析 LRC 格式的歌词时间轴与文本数据
  - [x] 实现 `LyricsService` 支持本地 / 已确认缓存优先读取，并在用户开启权限后搜索在线候选
  - [x] 使用新的 `LyricsCacheV2` 命名空间，不继承旧版可能未经确认的自动匹配结果
  - [x] 实现 `LyricsViewModel` 区分未找到、联网关闭和请求失败，支持重试、重新匹配与手动导入本地 LRC
  - [x] 在 `MusicExpandedView` 卡片区域新增 Tabs 切换展示，实现自动滚动及在线候选选择的 `MusicLyricsView`
- [x] 重构收起态右侧为极简悬浮式 5 栏高质感高低音律动霓虹频谱仪 (Premium Floating Equalizer)：
  - [x] 彻底消除底部实线，完美实现 2 个状态：暂停/闲时缩回成 5 个仅高 1.5pt、圆润精致的静态悬浮晶体点；播放中 5 根翡翠荧光绿霓虹音量条直接从晶体点处向上弹性跳跃，科幻灵动
  - [x] 多重谐波物理模拟算法，从最左低音 (Bass) 的声学重击到最右高音 (Treble) 的极速沙锤微震，完美呈献声律细节
  - [x] 双层发光晕染设计，呈现翠绿柔和的霓虹环境发光氛围，视觉重心极稳
  - [x] 提供高精度收起态 5 栏翡翠霓虹晶柱律动，闲时极简缩回成静态晶体点，播放中弹性跳跃。

- [x] 修复播放列表和快速启动的拖拽判定失效问题：通过 `DropFrameReporter` 在 `NSHostingView` 本地坐标系中上报实际热区，避免无标题栏 `NSPanel` 下 SwiftUI 全局坐标不稳定导致的判定失效。
- [x] 支持拖拽添加歌曲 / 文件夹
  - [x] `IslandHostingView` 接收本地 `fileURL` 拖拽，规避非激活 `NSPanel` 下 SwiftUI drop 不稳定的问题
  - [x] 拖拽文件和文件夹复用 ViewModel 的过滤、扫描和去重逻辑
  - [x] 空播放列表状态明确提示可拖拽歌曲或文件夹
  - [x] 拖拽悬停时显示 drop target 视觉提示
  - [x] 修复沙盒（Sandboxing）下拖拽单首歌曲悬停时由于 `isRegularFile` 检查需要获取 Security-Scoped 资源权限抛出异常，导致沙盒直接拦截并阻止拖拽进入（不显示虚线热感区）的 Bug。通过在拖拽悬停判断时仅检查后缀及 UTType 识别，而在最终 Drop 导入（已获授权）阶段再精细调用 `isRegularFile` 进行双向保障，实现 100% 毫秒级稳定拖拽体验。
- [x] 支持播放列表轻量持久化
  - [x] 保存音频文件 URL、只读 security-scoped bookmark、当前选中索引和播放模式
  - [x] 保存当前曲目播放进度，退出时写入最新播放位置
  - [x] 启动后恢复播放列表，seek 到上次位置并继续播放
  - [x] 恢复时过滤已不存在或不再是音频文件的路径
  - [x] `currentTrackProgress` 字段缺失时使用默认值 0
- [x] 支持快速启动 App 面板
  - [x] 快速启动常驻显示 10 个固定可空槽位，拖入 `.app` 时高亮目标槽
  - [x] 每次仅接收一个外部 App，拖到空槽放置、拖到已有图标替换；已有 App 使用系统原生拖拽图像跟随鼠标，拖到空槽移动、拖到占用槽交换
  - [x] 槽位布局写入持久化存储并在启动后恢复，中间空位不会被压缩
  - [x] 快速启动 App 上限调整为 10 个
  - [x] expanded 底部提供快捷启动布局开关；关闭时隐藏快捷启动、调整内容高度并停止接收 `.app` 投放，重新开启后恢复已保存槽位
- [x] 文档存放整理
  - [x] 注释规则收敛到 `docs/ARCHITECTURE.md`
  - [x] `README.md` 精简为项目入口和文档索引
  - [x] `docs/DESIGN.md` 移除重复文件职责说明
  - [x] `docs/TASKS.md` 精简为任务清单
  - [x] 根目录参考图片移动到 `docs/assets/`
- [x] 调整 App 外点击行为
  - [x] 自定义 hosting view 过滤 expanded 透明区域 hit-test
  - [x] 点击桌面或其它 App 不再触发收起
- [x] 修复添加歌曲面板被 island 覆盖与主线程阻塞问题
  - [x] 不隐藏、不移动顶部 island，重构为非阻塞异步模式，采用 Swift Concurrency `async/await` 异步置顶文件选择面板，防止主线程卡死
- [x] 播放中 Mascot 音符状态
  - [x] `MascotState.music` 由播放状态触发
  - [x] 共用音符特效保持环绕角色，数量按周期在 4 到 6 个之间变化
  - [x] 音符位置按周期错位变化，不引入音乐业务逻辑
- [x] 收起态右侧区域改为音乐活动指示，展开后恢复静音 / 设置 / 退出三个按钮
- [x] 代码全量重构与性能美学优化 (Premium Optimization)
  - [x] 重构 `MusicTimelineSlider` 滑块手势交互：拖拽中更新局部状态和临时时间文字，仅在松开时触发 `AVPlayer.seek(to:)`，解决高频 seek 噪音和掉帧问题
  - [x] 升级配色体系，替换纯红、绿、蓝、橙基色为荧光绿、珊瑚红、琥珀金和靛蓝，极大提升视觉质感
  - [x] 优化并全局修复 `.onHover` 指针改变逻辑：在 `islandContainer` 以及 `MusicExpandedView` 根容器上统一挂载 arrow 指针设置，彻底杜绝文本悬停处出现 I-beam 输入状态光标的问题
- [x] 旧版 7 款矢量角色重塑记录已归档为历史阶段：
  - [x] 当前源码不再保留 `SleepCapsuleMascotView`、`CatMascotView`、`NoodleMascotView`、`BirbMascotView` 和 `MoaiMascotView`
  - [x] 当前角色系统以 `RobotMascotView`、`GhostMascotView` 和 5 个 `Pixel*MascotView` 为准
- [x] 完成 expanded 底部「临时暂存」首版验收，用于临时收集文件或文件夹并随后拖出使用
  - [x] 放置在快捷启动下方的现有留白区域，保持 expanded 单层 island 壳体和整体高度不变，且通过高度扩展拉伸间距至精致的 38pt 像素高度
  - [x] 空态文案说明“直接拖入为移动，按 Option 为复制”“稍后可拖出到 Finder 或其他 App”
  - [x] 接收单个或多个文件、文件夹以及混合拖入；拖入仅记录当前会话引用，不移动、不复制、不缓存文件内容
  - [x] 每个暂存项使用 Quick Look 预览缩略图优先、系统图标回退、原始名称在下的横向图标架展示，长名称中间截断，并通过提示显示完整信息
  - [x] 相同路径去重；首版最多保留 `20` 个项目，超出容量时给予明确反馈
  - [x] 支持通过系统原生拖拽将单个项目拖出到 Finder 或其他接收文件的 App，成功拖出后自动移除对应暂存项，取消或投回自身时保留
  - [x] 支持单项移除与清空临时暂存；移除或清空仅删除引用，不删除磁盘上的原文件
  - [x] 外部已移动或删除的文件显示不可用状态，并允许从临时暂存移除
  - [x] 临时暂存首版仅保留当前 App 会话状态，退出重启后为空；暂不实现 bookmark 持久化恢复
  - [x] 明确三区拖拽路由：播放列表接收音乐导入、快捷启动接收 `.app` 配置、临时暂存接收任意文件引用
  - [x] 重新核定快捷启动开关关闭时的列表 / 歌词扩展高度，避免与固定显示的临时暂存布局冲突
  - [x] 实现 Finder 风格多选与批量拖出：单击单选、`Command` 增减选择、`Shift` 连续范围选择、空白区域框选（优化为独立 `.overlay` 渲染层，彻底解决 click/mouseDown 基线对齐抖动问题）、拖动任意已选项时整体原生拖出
  - [x] 拖入时记录移动 / 复制模式并在暂存项图标角标中反馈；为了极简扁平化，设计为高对比度红绿底黑字、无边线、常规字重（不加粗）徽章，且“移”采用珊瑚红高对比背景（0.88 opacity），“拷”采用翡翠绿高对比背景（0.88 opacity），拖拽释放后自动移除，不要求用户额外修饰
  - [x] 移 / 拷混合选择时即时提示“移 / 拷暂不支持混合，请分开拖出”，并阻止混合批量拖出
  - [x] 多项拖出显示叠放预览与数量角标；目标成功接收后批量移除对应引用，取消拖拽或投回自身时保留
  - [x] 空白区域拖动用于框选，不引入复选框或独立 management 模式；为保持非激活 `NSPanel` 不绑定需获取键盘焦点的 `Esc`
  - [x] 将临时暂存从 `Music/` 拆分为独立 `TemporaryTray/` 功能目录，音乐视图只负责在 expanded 布局中组合入口
  - [x] 将快捷启动从 `Music/` 拆分为独立 `QuickLaunch/` 功能目录，音乐视图只负责在 expanded 布局中组合入口
  - [x] 按职责拆分快捷启动和临时暂存：系统 API / 持久化 / 原生拖拽进入 Service 或 Controller，View 仅保留展示和意图转发
  - [x] 将播放列表、快捷启动与临时暂存的跨区域 AppKit 投放分发从窗口控制文件提取为独立拖放协调器
  - [x] 将 expanded 音乐视图中的播放列表卡片、列表行、进度条与头部微型按钮拆为独立音乐子视图文件
  - [x] 将播放列表拖入判定中的文件系统访问收口到 `MusicDirectoryScanner`，ViewModel 仅编排导入结果
  - [x] 快捷启动完全为空时显示主副引导文案；已有 App 或拖入悬停时恢复显示 10 个槽位占位
  - [x] 运行应用后完成视觉、三区拖放与重启空态验收
- [x] 完成 expanded 高度与底部拖入体验微调验收
  - [x] 普通 expanded 基础高度收缩为 `570`，保持顶部贴边和单层壳体
  - [x] 关闭快捷启动或临时暂存时按功能实际占用收缩高度，并对 expanded 高度做整数对齐，避免 1pt 贴顶跳动
  - [x] 临时暂存拖入热区和虚线反馈贴近底部壳体内边，同时不影响底部圆角和外侧阴影
  - [x] 静态微调 8 种开关组合下底部虚线框的完美对齐，统一保留 **`4pt`** 高级感精致呼吸边距，彻底消除底部多余空黑
  - [x] 关闭临时暂存后 expanded 继续向上收缩，减少底部空白
  - [x] 支持 macOS Sequoia 15.0+ 部署目标，解决编译器与交互适配 Bug：
    - [x] 将项目部署目标 `MACOSX_DEPLOYMENT_TARGET` 从 `26.4` 降低至 `15.0`，全面支持 macOS Sequoia 15.7.7 / 15.7.8。
    - [x] 重写设置页面 `SettingsHostingView` 为非泛型 `NSHostingView<AnyView>` 避让方案，完美绕过 Xcode 在 macOS 15 部署目标下针对泛型容器编译优化发生的 `EarlyPerfInliner` 编译器崩溃 Bug。
    - [x] 在 `ContentView.swift` 中引入条件编译指令 `#if arch(x86_64)`，在 Intel 芯片设备上应用 80 毫秒的展开动画阶段延迟，避开显卡缓冲重绘瓶颈；而在 Apple Silicon 设备上保持 0 延迟秒开，性能各取最优。
    - [x] 重构空白区域收起手势，移除 `ExpandedIslandView` 中被阻挡的底层手势层，将其统一移至 `MusicExpandedView` 根容器 `VStack` 的背景点击中，彻底解决全架构下点击灵动岛空白区域不收起的交互缺陷。
    - [x] 曾提供经过 lipo/file 验证的 Intel 专用包；压缩包作为本地交付产物，不保留在仓库根目录。

## 下一步任务

- [ ] 如继续扩展 Mascot，保持 `MascotKind` + `MascotView` + `Characters/` 的现有模式
- [x] 性能与 GPU 渲染优化：
  - [x] 关闭设置窗口原生阴影，解决设置窗口覆盖 island 时已复现的 GPU 增长
  - [x] 关闭设置窗口原生阴影后，根据交互需求恢复设置页 Mascot `idle` 动画预览

## 约束

- 每次只做一个明确的小任务
- 不做大规模重构
- 不删除用户未确认要删除的源码文件
- SwiftUI 为主，必要时再补 AppKit
- 编译验证：除非用户明确且主动地要求，否则在对话周期内无需主动运行任何编译验证指令（如 `xcodebuild`），以使协作流程保持极致轻量、顺畅且互不打扰
- Build / xcodebuild 规则以 `docs/ARCHITECTURE.md` 为准
- 新功能涉及系统 API、文件、播放器、权限或通知时，能力放入 Service / Controller，不写进 View
- 主 View 只负责组合 UI 和转发用户意图，ViewModel 负责状态和动作，Model 负责数据结构
- 文件职责、数据流、注释规则以 `docs/ARCHITECTURE.md` 为准
- 视觉和交互规则以 `docs/DESIGN.md` 为准

## 验收清单

- [x] App 启动后收起态 compact 仍显示 Mascot / 右侧音乐活动指示
- [x] 播放中收起态 compact 仍显示 Mascot / 右侧音乐活动指示，不显示音乐控制
- [x] expanded 显示 mini player：曲名、播放状态、播放 / 暂停、添加歌曲、播放模式、播放列表、进度条、当前时间 / 总时长
- [x] 播放列表支持添加本地文件和目录
- [x] 播放列表支持拖拽添加本地音频文件和文件夹
- [x] 播放列表支持轻量持久化，重启后恢复列表、当前索引、播放模式、文件访问权限和上次播放进度；恢复时失效 URL 被过滤后不会把 `currentIndex` 指向错误歌曲
- [x] 恢复后 seek 到上次位置并继续播放
- [x] 播放列表支持点击播放、删除单项、清空列表、自动下一首、上一首 / 下一首
- [x] 播放模式可在顺序、单曲循环、列表循环、随机之间切换；本地和在线歌曲自然结束时的单曲循环都会重播当前曲目
- [x] 线上发现歌曲列表和播放失败状态只在当前 App 会话内缓存，退出或刷新后清空；本地播放列表持久化不保存线上发现列表或失败状态
- [x] 快速启动空态显示引导文案，已有 App 或拖入悬停时显示 10 个可空槽位、拖入高亮目标槽、外部目标槽替换、已有 App 移动 / 交换和槽位布局持久化
- [x] 收起态右侧显示音乐活动指示，展开后恢复三个工具按钮
- [x] `ContentView.swift` 不直接操作 `AVPlayer`
- [x] `IslandViewModel.swift` 不包含音乐业务逻辑
- [x] `AVPlayer` 只在 `MusicPlayerController.swift` 中出现
- [x] `NSOpenPanel` 只在 `MusicFilePicker.swift` 中出现
- [x] `FloatingPanelController.swift` 的窗口定位和展开 / 收起逻辑保持独立
- [x] Mascot 动画文件不承担音乐业务逻辑
- [x] 本次涉及的复杂逻辑和关键代码已按有价值优先标准补充中文注释
- [x] 歌词加载与展示：
  - [x] 当前曲目加载、expanded / 歌词 UI 或遥控状态读取时优先读取本地 `.lrc` 或已确认缓存；联网仍受“联网匹配歌词”开关控制
  - [x] “联网匹配歌词”默认关闭；开启联网后若无本地歌词，自动下载缓存限制为标题 / 歌手高可信匹配，低可信结果回退候选选择
  - [x] 同目录歌词受沙盒授权限制时，可手动选择 `.lrc` 文件并缓存当前关联
  - [x] 区分无歌词与网络失败，失败可重试；已有结果点击重新匹配时由用户选择候选或返回原歌词
  - [x] 当前播放进度行高亮，拖动进度条即时同步
  - [x] 关闭播放列表时的单行歌词：采用小一号且常规字重（10pt, regular）展示，以 20pt 间距在底部悬浮显示，规避圆角裁剪且不影响其它 UI 布局与窗体阴影。
- [x] 临时暂存首版验收：
  - [x] 临时暂存为空时显示“直接拖入为移动，按 Option 为复制”和“稍后可拖出到 Finder 或其他 App”的引导文案；有项目后切换为横向图标架
  - [x] 拖入单个或多个文件 / 文件夹后以预览缩略图优先、系统图标回退、原始名称在下的横向图标架显示，源项目不移动、不复制
  - [x] 普通文件、音频文件与 `.app` 拖入临时暂存时都仅形成临时引用，不触发其它区域动作
  - [x] 暂存项目可原生拖出到 Finder 或其他 App，成功拖出后对应条目自动移除，取消拖拽或投回自身时仍保留
  - [x] 支持 `Command` / `Shift` 和框选选择多个暂存项目并一次性拖出；选中态、叠放拖拽预览与数量反馈清晰
  - [x] 移 / 拷混合选择时提示分开拖出；批量成功拖出仅移除被拖出的对应引用，且 island 保持非激活窗口行为
  - [x] 单项移除与清空不会删除原文件，失效项目可被识别并移除
  - [x] 收起再展开后当前会话条目仍在，App 重启后首版临时暂存为空
